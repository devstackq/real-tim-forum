export default class {
    constructor(params) {
        this.params = params;
    }

    setTitle(title) {
        document.title = title;
        console.log('main page, get all post')
    }

    async getHtml() {
        //show list last created post, filter page
        return "";
    }
}