export default class {
    constructor(params) {
        this.params = params;
    }
    setTitle(title) {
        document.title = title;
    }
//jsx
    async getHtml() {
        //get -> set profile data -> by id
        //fetch - post, edit -> name etc
        return "";
    
    }
}